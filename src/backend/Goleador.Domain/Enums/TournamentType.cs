namespace Goleador.Domain.Enums;

public enum TournamentType
{
    RoundRobin = 0,   // Girone all'italiana (tutti contro tutti)
    Elimination = 1   // Eliminazione diretta (tabellone tennistico)
}
